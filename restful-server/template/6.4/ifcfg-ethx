DEVICE="{{DEVICE}}"
BOOTPROTO="{{BOOTPROTO}}"
GATEWAY="{{GATEWAY}}"
HWADDR="{{HWADDR}}"
IPADDR="{{IPADDR}}"
NETMASK="{{NETMASK}}"
NM_CONTROLLED="yes"
ONBOOT="{{ONBOOT}}"
TYPE="Ethernet"
ETHTOOL_OPTS="{{ETHTOOL_OPTS}}"
